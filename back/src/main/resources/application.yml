spring:
  application:
    name: back
  datasource:
    url: jdbc:mysql://localhost:3306/your_car_your_way
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
      # properties:
      # hibernate:
      # dialect: org.hibernate.dialect.MySQL5Dialect
    # enable_lazy_load_no_trans: true
server:
  port: 8080